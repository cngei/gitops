apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
  namespace: auth
spec:
  instances: 3
  image: ghcr.io/cngei/kc-theme:f31e0fc9d2d62b3bf7ef92bf5a3cb5c70dd5cba3
  startOptimized: false
  db:
    vendor: postgres
    host: pg-cluster-rw.db.svc.cluster.local
    port: 5432
    database: keycloak
    schema: public
    usernameSecret:
      name: pg-cluster-keycloak
      key: username
    passwordSecret:
      name: pg-cluster-keycloak
      key: password
  hostname:
    hostname: https://auth.cngei.it/auth
  proxy:
    headers: xforwarded
  http:
    httpEnabled: true
  additionalOptions:
    - name: http-relative-path
      value: "auth"
